#include <iostream>

struct A{};

class B{};

int main()
{

	float f();

	int a;

	double b;

	int c;

	return 0;
	
	/*Перемещение отличается от слияния тем, что она перезаписывает историю, потому что передает завершенную работу из одной ветки в другую.
В то время как merge принимает содержимое ветки источника и объединяет их с целевой веткой. В этом процессе изменяется только целевая ветка. История исходных веток остается неизменной.*/
}
